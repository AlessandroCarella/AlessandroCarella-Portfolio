// Single source of truth for the site's absolute base URL.
// Change this ONE value when the custom domain goes live. No trailing slash —
// route paths like "/home" are appended to it.
// Consumed by: vite.config.js (index.html %SITE_URL%), src/components/Seo.jsx,
// src/components/projects/ProjectContent.jsx, scripts/gen-seo-files.mjs.
export const SITE_URL = "https://alessandrocarella.pages.dev";
